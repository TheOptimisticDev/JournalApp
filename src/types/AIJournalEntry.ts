// src/types/AIJournalEntry.ts

export interface AIJournalEntry {
  id: string;           // Unique identifier for the journal entry
  content: string;      // The text content of the journal entry
  createdAt: string;    // ISO date string when the entry was created
}
    